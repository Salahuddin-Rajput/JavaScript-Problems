var num;
function square(num){

return num*num;

}
console.log("Square of 4", square(4));
console.log("Square of 5", square(5));
console.log("Square of 6", square(6));
//How JS works:-
//JavaScript being a single-threaded language means that it has a single call stack, meaning it can only execute one command or piece of code at a time. In other words, JavaScript processes tasks sequentially, one after the other, in the order they appear in the cod